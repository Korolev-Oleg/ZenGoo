# Zenmoney to Googlesheet
> python

### install and run on linux
```shell script
python -m venv venv
. ./venv/bin/activate
pip install -r requirements.txt
python integration.py
``` 

### Install and run on window
```cmd
start SETUP.bat
```
